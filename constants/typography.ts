// Fonte display da marca (títulos e wordmark). O corpo do app segue na fonte
// de sistema — o tema (constants/themes.ts) continua só de cores.
export const FONTS = {
  display: 'PlayfairDisplay_700Bold',
  displayBlack: 'PlayfairDisplay_900Black',
} as const;
